/**
 * SourceChunks.jsx
 * ----------------
 * Source chunk display has been disabled.
 * The backend still returns source chunks internally,
 * but they are intentionally not shown in the UI.
 */

export default function SourceChunks() {
  return null;
}